[Unit]
Description="Start pg-promise node application"
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/node "{{ dir }}/src/server.js"
Restart=on-failure

[Install]
WantedBy=multi-user.target

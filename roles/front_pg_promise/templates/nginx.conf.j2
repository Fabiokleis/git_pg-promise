server {
    listen 3000;

    root {{ static_files_dir }}/front_pg-promise;
    index index.html index.htm;
    
    server_name {{ server_name }};

    location / {
        default_type "text/html";
        try_files $uri.html $uri $uri/ =404;
    }
}
